{
  "name": "projeto-marco",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "pretest": "playwright install chromium",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
