{
  "name": "projeto-marco",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "pretest": "playwright install chromium",
    "test": "playwright test",
    "preview:capture": "node tests/visual/capture-preview.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
