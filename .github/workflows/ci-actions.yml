name: CI Actions

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  lint-workflows:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run actionlint
        uses: rhysd/actionlint@v1
